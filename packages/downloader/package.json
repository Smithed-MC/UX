{
  "name": "downloader",
  "version": "0.1.0",
  "description": "",
  "main": "index.ts",
  "types": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "npm run build && tsc",
    "build": "node build.js"
  },
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@smithed-mc/data-types": "*",
    "database": "*"
  }
}
